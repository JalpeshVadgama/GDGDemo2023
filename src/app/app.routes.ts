import { Routes } from '@angular/router';
import { PersonListWithoutPipeComponent } from './person-list-without-pipe/person-list-without-pipe.component';

export const routes: Routes = [
    {
      path: "personlist-without-pipe",
      component: PersonListWithoutPipeComponent
    }
];
